{
    "version": 2,
    "builds": [{
        "src": "./src/apps.ts",
        "use": "@vercel/node"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/src/apps.ts"
        }
    ]
}